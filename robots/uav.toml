# UAVs flying PX4 (e.g. via mavros / pymavlink).

[robot_types.uav]
display_name = "UAV"

[robot_types.uav.actions.arm]
command = "echo TODO"
timeout_sec = 60
dangerous = true

[robot_types.uav.actions.disarm]
command = "echo TODO"
timeout_sec = 60
dangerous = true

